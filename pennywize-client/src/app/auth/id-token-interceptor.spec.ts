import { IdTokenInterceptor } from './id-token-interceptor';

describe('IdTokenInterceptor', () => {
  it('should create an instance', () => {
    expect(new IdTokenInterceptor()).toBeTruthy();
  });
});
