name: TEST

on:
  push:
    branches: [ actions-test ]

defaults:
  shell: bash -eu

jobs:
  test-unbound-variable:
    runs-on: ubuntu-latest

    steps:
    - name: let's see if throws
      run: |
        echo something
        echo $NONEXISTING_VAR
        echo something else